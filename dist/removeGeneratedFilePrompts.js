"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeGeneratedFilePrompts = void 0;
const fs_1 = __importDefault(require("fs"));
const promptFilePathToTargetFilePath_1 = require("./promptFilePathToTargetFilePath");
const removeGeneratedFilePrompts = (prompts) => {
    // check if the file to be generated by the prompt already exists
    const remaingingFiles = Object.keys(prompts).filter((path) => !fs_1.default.existsSync((0, promptFilePathToTargetFilePath_1.promptFilePathToTargetFilePath)(path)));
    // remove the generated files
    const remainingPrompts = {};
    remaingingFiles.forEach((path) => {
        remainingPrompts[path] = prompts[path];
    });
    return remainingPrompts;
};
exports.removeGeneratedFilePrompts = removeGeneratedFilePrompts;
